/**
 * A button that copies text to clipboard.
 */
import {CopyButton} from "@corensystem/coren-ui/copy-button";

export function Default() {
	return <CopyButton value="npm install @corensystem/coren-ui" />;
}
