/**
 * Provide descriptive labels for accessibility.
 */
import {CopyButton} from "@corensystem/coren-ui/copy-button";

export function LabelDo() {
	return <CopyButton value="user@example.com" label="Copy email address" />;
}
