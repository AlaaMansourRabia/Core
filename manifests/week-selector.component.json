{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "week-selector",
	"tier": "component",
	"name": "WeekSelector",
	"category": "form",
	"version": "1.0.0",
	"manifestStatus": "stub",
	"source": {
		"path": "packages/components/src/week-selector.tsx",
		"import": "@corensystem/coren-ui/week-selector",
		"export": "WeekSelector"
	},
	"tags": [
		"week",
		"selector",
		"date",
		"range"
	],
	"intent": "A compact week selector: a clickable date-range label that opens a calendar to jump to any week, previous / next chevrons that step the selected week (keeping it centered in the visible tabs), and a segmented row of week tabs (built on the shared `Tabs` component). Controlled or uncontrolled via `value` / `defaultValue`.",
	"when": [],
	"chooseOver": [],
	"pairsWith": [],
	"requiredProviders": [],
	"commonMistakes": [],
	"lineage": {
		"origin": "migrated",
		"authoredBy": "Group C of the missing-widgets template review (2026-08-26)",
		"derivedFrom": {
			"source": null,
			"extracted": []
		},
		"status": "accepted",
		"verifiedBy": null,
		"confidence": "unset — intent taken verbatim from the module's own doc comment; contract not yet enumerated",
		"supersedes": null,
		"lastValidated": "2026-08-26",
		"notes": [
			"Was exported from @corensystem/coren-ui with no manifest at all, so it did not exist to the builder, to library-index.json, or to any agent selecting components.",
			"Stub by design: the intent is accurate, the data contract is not yet authored.",
			"pilot: false"
		]
	},
	"docs": {
		"props": [
			{
				"name": "weeks",
				"type": "WeekSelectorWeek[]",
				"required": true,
				"description": "The full list of weeks. The tabs show a window of these, centered on the selected week."
			},
			{
				"name": "value",
				"type": "string",
				"required": false,
				"description": "Selected week value (controlled)."
			},
			{
				"name": "defaultValue",
				"type": "string",
				"required": false,
				"description": "Initial selected week value (uncontrolled). Defaults to the latest (last) week."
			},
			{
				"name": "visibleCount",
				"type": "number",
				"required": false,
				"description": "Number of week tabs shown at once, centered on the selection (clamped at the ends). Default 4."
			},
			{
				"name": "dateLabel",
				"type": "React.ReactNode",
				"required": false,
				"description": "Override the date-range label. Defaults to the selected week's start–end range."
			},
			{
				"name": "bare",
				"type": "boolean",
				"required": false,
				"description": "Render bare: drop the default outer container (rounded corners, border, card background, and padding) so the control sits flush inside a toolbar/app bar. The inner layout — date-range label, chevrons, and week tabs — is unchanged. Default false."
			},
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "asChild",
				"type": "boolean",
				"default": "false",
				"required": false,
				"description": "Render as child element, merging props and behavior."
			},
			{
				"name": "disabled",
				"type": "boolean",
				"default": "false",
				"required": false,
				"description": "Whether the component is disabled."
			},
			{
				"name": "...props",
				"type": "React.HTMLAttributes<HTMLDivElement>",
				"required": false,
				"description": "All standard HTML attributes are supported."
			}
		],
		"usage": [
			"Use WeekSelector when you need to collect user input or allow users to make selections. This component provides a consistent interaction pattern that users will find familiar and accessible.",
			"WeekSelector should be used within forms or interactive interfaces where user data collection is required. Ensure proper labeling and validation feedback for accessibility. Avoid using this component for read-only or display-only content."
		],
		"anatomy": {
			"description": "WeekSelector is composed of the following parts:",
			"parts": [
				{
					"name": "WeekSelector",
					"description": "Root WeekSelector element."
				},
				{
					"name": "WeekSelectorContent",
					"description": "Content area of the WeekSelector."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Use semantic props",
				"description": "Use the component's built-in props rather than className overrides.",
				"example": "<WeekSelector variant=\"primary\" size=\"lg\" />"
			},
			{
				"type": "dont",
				"title": "Don't override core styles",
				"description": "Avoid using className to override the component's fundamental styling.",
				"example": "<WeekSelector className=\"custom-bg-color\" />"
			},
			{
				"type": "do",
				"title": "Provide accessible labels",
				"description": "Include proper ARIA labels for screen reader users.",
				"example": "<WeekSelector aria-label=\"Description\" />"
			},
			{
				"type": "dont",
				"title": "Don't skip accessibility",
				"description": "Never omit accessibility attributes for interactive components.",
				"example": "<WeekSelector /> // No aria-label"
			},
			{
				"type": "do",
				"title": "Handle all states",
				"description": "Account for loading, error, empty, and success states.",
				"example": "<WeekSelector loading={isLoading} error={error} />"
			},
			{
				"type": "dont",
				"title": "Don't ignore edge cases",
				"description": "Avoid showing broken UI when data is missing or loading.",
				"example": "<WeekSelector>{data.value}</WeekSelector> // Crashes if data is undefined"
			}
		]
	}
}
