{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "context-menu",
	"tier": "component",
	"name": "ContextMenu",
	"category": "overlay",
	"version": "1.0.1",
	"manifestStatus": "complete",
	"source": {
		"path": "packages/components/src/context-menu.tsx",
		"import": "@corensystem/core-ui/context-menu",
		"export": "ContextMenu"
	},
	"tags": [
		"overlay",
		"menu",
		"context",
		"rightclick"
	],
	"intent": "Right-click (context) menu of actions scoped to the element it wraps. Items highlight on pointer or keyboard focus using the --menu-highlight token and carry a pointer cursor.",
	"when": [
		"Right-click actions on rows, cards, or canvas items",
		"Desktop power-user shortcuts on a target"
	],
	"chooseOver": [
		{
			"component": "DropdownMenu",
			"because": "Same menu UI, but DropdownMenu uses an explicit trigger while ContextMenu uses right-click on the wrapped element"
		}
	],
	"pairsWith": [
		"TreeRow",
		"Card"
	],
	"requiredProviders": [],
	"commonMistakes": [
		{
			"mistake": "Rely on it as the only, discoverable action surface",
			"fix": "Also expose a visible DropdownMenu",
			"avoidOverride": "Rely on it as the only, discoverable action surface — also expose a visible DropdownMenu"
		}
	],
	"lineage": {
		"origin": "migrated",
		"authoredBy": "A1 component migration batch (2026-07)",
		"derivedFrom": {
			"source": "packages/components/src/context-menu.tsx",
			"extracted": [
				"cva variants/sizes remain generated by the structural extractor"
			]
		},
		"status": "accepted",
		"verifiedBy": null,
		"confidence": "unset — projection round-trip verified; no held-out eval yet",
		"supersedes": null,
		"lastValidated": "2026-09-13",
		"notes": [
			"migrated from library-index.json (hand-authored catalog entry)"
		]
	}
}
