# Multi-surface negative fixture

This minimized reproduction preserves the second experiment's failure modes: an unpaired/custom shell,
primitive reconstruction of table/chart/canvas regions, a parallel Studio shell, forced reload navigation,
reference-palette aliases, a nonexistent token, an incompatible color wrapper, raw TSX/SVG colors, internal
Core selector overrides, a hybrid tab variant, owned-shell visual overrides, and fixed application geometry.
It also captures the screenshot experiment's accidental shell gutter and document scrolling, redundant
DataTable surface, duplicate chat composer, fabricated back navigation, and decorative canvas controls.
Contract v2, route-adoption, token semantics/provenance, CSS ownership/variant integrity, interaction semantics,
and runtime-audit regressions use these signals as negative evidence.
