/**
 * Default tree row.
 */
import {TreeRow, TreeRowLabel} from "@corensystem/coren-ui/tree-row";

export function Default() {
	return (
		<TreeRow>
			<TreeRowLabel>Root Item</TreeRowLabel>
		</TreeRow>
	);
}
