/**
 * Copy button with custom accessible label.
 */
import {CopyButton} from "@corensystem/coren-ui/copy-button";

export function WithLabel() {
	return <CopyButton value="ABC123" label="Copy invite code" />;
}
