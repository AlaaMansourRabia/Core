/**
 * Canonical Timesheet template export.
 *
 * VerifyTimeCommandCenter remains available from its original module for
 * backwards compatibility with existing consumers.
 *
 * @deprecated Preview/demo prototype — renders built-in sample data and mock handlers. Not a
 * supported production import: compose your page from the widgets this template uses, wired to your
 * own data (see the CoreTimesheet template docs in Storybook). Slated for removal from the public API in a
 * future major.
 */
export {VerifyTimeCommandCenter as Timesheet} from "./core-verifytime-command-center";
