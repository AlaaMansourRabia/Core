{
	"name": "@corensystem/mcp",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "Core MCP server — the tool-agnostic intelligence API any AI editor consults to build UI from Core (resolve/validate/generate). Wraps @core/sdk over stdio.",
	"bin": {
		"core-mcp": "./src/server.mjs"
	},
	"exports": {
		".": "./src/server.mjs",
		"./tools": "./src/tools.mjs"
	},
	"files": [
		"src"
	],
	"dependencies": {
		"@modelcontextprotocol/sdk": "1.29.0",
		"@corensystem/sdk": "workspace:*"
	}
}
