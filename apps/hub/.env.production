# Production (hosted at core.wakecap.com). The Developer Access page points at the hosted MCP, and the
# embedded tabs point at same-origin subpaths served from this same deployment. Studio has no hosted
# URL (needs a local backend) so its tab is hidden in the cloud.
VITE_WAKECORE_MCP_URL=https://core.wakecap.com/mcp
VITE_DESIGNER_URL=/designers/
VITE_STORYBOOK_URL=/storybook/
