{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "map-legend",
	"tier": "widget",
	"name": "MapLegend",
	"category": "map",
	"version": "1.0.0",
	"manifestStatus": "stub",
	"source": {
		"path": "packages/components/src/map-legend.tsx",
		"import": "@corensystem/coren-ui/map-legend"
	},
	"tags": [
		"map",
		"legend",
		"key",
		"overlay",
		"collapsible"
	],
	"intent": "Compact, foldable key for what is currently drawn on a map.",
	"when": [
		"A map carries more than one kind of marker or layer",
		"Marker colour encodes a dimension the viewer has to decode"
	],
	"chooseOver": [
		{
			"component": "Legend",
			"because": "Legend is a chart key laid out inline with its chart. MapLegend floats over a map, folds away, and draws swatches shaped like the markers they describe."
		}
	],
	"pairsWith": [
		"ObservationsMap",
		"MapControlPanel"
	],
	"requiredProviders": [],
	"commonMistakes": [
		"Drawing every swatch as the same shape — a key is easier to read when a round entry means a round marker and a dashed entry means a dashed outline.",
		"Letting the legend describe a scheme the map is not currently using — drive its sections from the same state that colours the markers.",
		"Reaching for a heavy drop shadow on a floating map surface — it reads as a modal over the map instead of a panel on it. Use the shared FLOAT_SHADOW."
	],
	"stubPlan": [
		"dataContract: stub — author input fields, slots, rowIdentity",
		"configSchema: stub — enumerate config props from source",
		"states: stub — list default/empty/loading/etc.",
		"composedOf: stub — list composed components",
		"evaluationCriteria: stub — define selection/config/render checks"
	],
	"lineage": {
		"origin": "authored",
		"authoredBy": "Observations-Map-View",
		"derivedFrom": {
			"source": null,
			"extracted": []
		},
		"status": "accepted",
		"verifiedBy": null,
		"confidence": "unset — rendered and read back in the browser",
		"supersedes": null,
		"lastValidated": "2026-08-31",
		"notes": []
	},
	"composedOf": [
		"Button"
	],
	"docs": {
		"usage": [
			"Use MapLegend to enhance your user interface. This component provides a consistent, accessible, and well-designed solution for this use case.",
			"MapLegend integrates seamlessly with other components in the design system. Follow the established patterns and guidelines to ensure consistency across your application."
		],
		"props": [
			{
				"name": "title",
				"type": "React.ReactNode",
				"required": false,
				"description": "Header text. Default \"Map legend\"."
			},
			{
				"name": "sections",
				"type": "MapLegendSection[]",
				"required": true,
				"description": ""
			},
			{
				"name": "defaultCollapsed",
				"type": "boolean",
				"required": false,
				"description": "Start folded to just the header. Default false."
			},
			{
				"name": "collapsed",
				"type": "boolean",
				"required": false,
				"description": ""
			},
			{
				"name": "width",
				"type": "number",
				"required": false,
				"description": "Width in px. Default `150` — a key, not a panel."
			},
			{
				"name": "collapsedWidth",
				"type": "number",
				"required": false,
				"description": "Width while collapsed. Default `160`, matching a collapsed `MapControlPanel`."
			},
			{
				"name": "maxHeight",
				"type": "string",
				"required": false,
				"description": "Cap on the card's height; the body scrolls beneath it. Default `\"50vh\"`."
			},
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "...props",
				"type": "React.HTMLAttributes<HTMLDivElement>",
				"required": false,
				"description": "All standard HTML attributes are supported."
			}
		],
		"anatomy": {
			"description": "MapLegend is composed of the following parts:",
			"parts": [
				{
					"name": "MapLegend",
					"description": "Root MapLegend element."
				},
				{
					"name": "MapLegendContent",
					"description": "Content area of the MapLegend."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Use semantic props",
				"description": "Use the component's built-in props rather than className overrides.",
				"example": "<MapLegend variant=\"primary\" size=\"lg\" />"
			},
			{
				"type": "dont",
				"title": "Don't override core styles",
				"description": "Avoid using className to override the component's fundamental styling.",
				"example": "<MapLegend className=\"custom-bg-color\" />"
			},
			{
				"type": "do",
				"title": "Provide accessible labels",
				"description": "Include proper ARIA labels for screen reader users.",
				"example": "<MapLegend aria-label=\"Description\" />"
			},
			{
				"type": "dont",
				"title": "Don't skip accessibility",
				"description": "Never omit accessibility attributes for interactive components.",
				"example": "<MapLegend /> // No aria-label"
			},
			{
				"type": "do",
				"title": "Handle all states",
				"description": "Account for loading, error, empty, and success states.",
				"example": "<MapLegend loading={isLoading} error={error} />"
			},
			{
				"type": "dont",
				"title": "Don't ignore edge cases",
				"description": "Avoid showing broken UI when data is missing or loading.",
				"example": "<MapLegend>{data.value}</MapLegend> // Crashes if data is undefined"
			}
		]
	}
}
