/**
 * Inline code with default styling.
 */
import {Code} from "@corensystem/coren-ui/code";

export function Default() {
	return (
		<p className="wwc:text-sm">
			Run <Code>npm install</Code> to install dependencies.
		</p>
	);
}
