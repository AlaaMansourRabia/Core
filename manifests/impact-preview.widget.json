{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "impact-preview",
	"tier": "widget",
	"name": "LineageImpactView",
	"category": "data",
	"version": "1.0.0",
	"manifestStatus": "complete",
	"source": {
		"path": "packages/components/src/pages/wc3-lineage-impact-view.tsx",
		"import": "@corensystem/coren-ui/pages/lineage-impact-view",
		"export": "LineageImpactView"
	},
	"tags": [
		"impact",
		"what-if",
		"blast-radius",
		"lineage",
		"ontology",
		"dependencies",
		"wc3"
	],
	"intent": "What-if blast radius: given a change to one ontology element, the pipelines, processes and actions it reaches.",
	"when": [
		"Before changing or deleting a shared ontology element",
		"Any admin surface answering \"what does this break\""
	],
	"whenNot": [
		"Tracing where data came FROM -> use the data lineage view",
		"Listing rule violations -> use the lint findings view"
	],
	"chooseOver": [
		{
			"component": "A dependency list",
			"because": "A list names the neighbours; this walks the graph and reports what a change actually reaches, across pipelines, processes and actions at once."
		}
	],
	"pairsWith": [
		"GraphCanvas",
		"Badge",
		"Card"
	],
	"requiredProviders": [],
	"dataContract": {
		"input": {
			"objectTypeId": {
				"type": "string",
				"required": false,
				"note": "Deep-linked object type; a routed id wins over the remembered one."
			},
			"onDetailChange": {
				"type": "(label: string | null) => void",
				"required": false,
				"note": "Reports the drill-in segment for a host breadcrumb."
			},
			"onNavigate": {
				"type": "(target: Wc3LineageNavTarget) => void",
				"required": false,
				"note": "Coarse by design — selects a perspective and a tab, not a record."
			},
			"onOpenChangeset": {
				"type": "() => void",
				"required": false
			}
		},
		"events": {
			"onNavigate": {
				"payload": {
					"target": "Wc3LineageNavTarget"
				}
			}
		},
		"rowIdentity": "Reached records are keyed by their own ids across three registries."
	},
	"configSchema": {},
	"slots": {},
	"states": {
		"unselected": "No element chosen yet.",
		"reached": "Pipelines, processes and actions the change would touch.",
		"isolated": "Nothing downstream depends on the element."
	},
	"composedOf": [
		"Card",
		"Badge",
		"GraphCanvas"
	],
	"allowedPlacement": [
		"a lineage perspective",
		"a delete confirmation flow"
	],
	"forbiddenPlacement": [
		"a dashboard tile"
	],
	"minimalExample": "import {LineageImpactView} from \"@corensystem/coren-ui/pages/lineage-impact-view\";\n\n<LineageImpactView objectTypeId=\"ot_permit\" onNavigate={goTo} />",
	"evaluationCriteria": [
		"All three downstream registries are walked, not just one."
	],
	"lineage": {
		"origin": "authored",
		"authoredBy": "Promoted from the WC3 lineage perspective (2026-08-26)",
		"derivedFrom": {
			"source": "packages/components/src/pages/wc3-lineage-impact-view.tsx",
			"extracted": [
				"blast-radius walk across pipelines, processes and actions"
			]
		},
		"status": "candidate",
		"verifiedBy": null,
		"confidence": "unset — no held-out eval yet",
		"supersedes": null,
		"lastValidated": "2026-08-26",
		"notes": [
			"Promoted in group B of the missing-widgets template review.",
			"pilot: true"
		]
	},
	"docs": {
		"usage": [
			"Use LineageImpactView to enhance your user interface. This component provides a consistent, accessible, and well-designed solution for this use case.",
			"LineageImpactView integrates seamlessly with other components in the design system. Follow the established patterns and guidelines to ensure consistency across your application."
		],
		"props": [
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "children",
				"type": "React.ReactNode",
				"required": true,
				"description": "Child elements to render."
			}
		],
		"anatomy": {
			"description": "LineageImpactView is composed of the following parts:",
			"parts": [
				{
					"name": "LineageImpactView",
					"description": "Root LineageImpactView element."
				},
				{
					"name": "LineageImpactViewContent",
					"description": "Content area of the LineageImpactView."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Use semantic props",
				"description": "Use the component's built-in props rather than className overrides.",
				"example": "<LineageImpactView variant=\"primary\" size=\"lg\" />"
			},
			{
				"type": "dont",
				"title": "Don't override core styles",
				"description": "Avoid using className to override the component's fundamental styling.",
				"example": "<LineageImpactView className=\"custom-bg-color\" />"
			},
			{
				"type": "do",
				"title": "Provide accessible labels",
				"description": "Include proper ARIA labels for screen reader users.",
				"example": "<LineageImpactView aria-label=\"Description\" />"
			},
			{
				"type": "dont",
				"title": "Don't skip accessibility",
				"description": "Never omit accessibility attributes for interactive components.",
				"example": "<LineageImpactView /> // No aria-label"
			},
			{
				"type": "do",
				"title": "Handle all states",
				"description": "Account for loading, error, empty, and success states.",
				"example": "<LineageImpactView loading={isLoading} error={error} />"
			},
			{
				"type": "dont",
				"title": "Don't ignore edge cases",
				"description": "Avoid showing broken UI when data is missing or loading.",
				"example": "<LineageImpactView>{data.value}</LineageImpactView> // Crashes if data is undefined"
			}
		]
	}
}
