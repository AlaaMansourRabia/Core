{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "pagination",
	"tier": "component",
	"name": "Pagination",
	"category": "navigation",
	"version": "1.0.0",
	"manifestStatus": "complete",
	"source": {
		"path": "packages/components/src/pagination.tsx",
		"import": "@corensystem/coren-ui/pagination",
		"export": "Pagination"
	},
	"tags": [
		"navigation",
		"pagination",
		"pages"
	],
	"intent": "Page-number navigation for paged lists.",
	"when": [
		"Server-paged tables or lists"
	],
	"chooseOver": [
		{
			"component": "DataTable",
			"because": "DataTable has built-in pagination for client-side data"
		}
	],
	"pairsWith": [],
	"requiredProviders": [],
	"lineage": {
		"origin": "migrated",
		"authoredBy": "A1 component migration batch (2026-07)",
		"derivedFrom": {
			"source": "packages/components/src/pagination.tsx",
			"extracted": [
				"cva variants/sizes remain generated by the structural extractor"
			]
		},
		"status": "accepted",
		"verifiedBy": null,
		"confidence": "unset — projection round-trip verified; no held-out eval yet",
		"supersedes": null,
		"lastValidated": "2026-07-05",
		"notes": [
			"migrated from library-index.json (hand-authored catalog entry)"
		]
	},
	"docs": {
		"props": [
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "...props",
				"type": "React.HTMLAttributes<HTMLElement>",
				"required": false,
				"description": "All standard HTML attributes are supported."
			}
		],
		"usage": [
			"Use Pagination to help users navigate through your application or content hierarchy. This component provides clear wayfinding and helps users understand their current location within the interface.",
			"Pagination is ideal for multi-page applications, content-heavy interfaces, or any situation where users need to move between different sections. Keep navigation patterns consistent across your application for better user experience."
		],
		"anatomy": {
			"description": "Pagination is composed of the following parts:",
			"parts": [
				{
					"name": "Pagination",
					"description": "Root Pagination element."
				},
				{
					"name": "PaginationContent",
					"description": "Content area of the Pagination."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Use semantic props",
				"description": "Use the component's built-in props rather than className overrides.",
				"example": "<Pagination variant=\"primary\" size=\"lg\" />"
			},
			{
				"type": "dont",
				"title": "Don't override core styles",
				"description": "Avoid using className to override the component's fundamental styling.",
				"example": "<Pagination className=\"custom-bg-color\" />"
			},
			{
				"type": "do",
				"title": "Provide accessible labels",
				"description": "Include proper ARIA labels for screen reader users.",
				"example": "<Pagination aria-label=\"Description\" />"
			},
			{
				"type": "dont",
				"title": "Don't skip accessibility",
				"description": "Never omit accessibility attributes for interactive components.",
				"example": "<Pagination /> // No aria-label"
			},
			{
				"type": "do",
				"title": "Handle all states",
				"description": "Account for loading, error, empty, and success states.",
				"example": "<Pagination loading={isLoading} error={error} />"
			},
			{
				"type": "dont",
				"title": "Don't ignore edge cases",
				"description": "Avoid showing broken UI when data is missing or loading.",
				"example": "<Pagination>{data.value}</Pagination> // Crashes if data is undefined"
			}
		]
	}
}
