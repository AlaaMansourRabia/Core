/**
 * Show what's happening.
 */
import {ThinkingPill} from "@corensystem/coren-ui/thinking-pill";

export function FeedbackDo() {
	return <ThinkingPill animated>Searching documents...</ThinkingPill>;
}
