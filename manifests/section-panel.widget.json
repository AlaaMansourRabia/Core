{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "section-panel",
	"tier": "widget",
	"name": "SectionPanel",
	"category": "layout",
	"version": "1.0.0",
	"manifestStatus": "complete",
	"source": {
		"path": "packages/components/src/section-panel.tsx",
		"import": "@corensystem/coren-ui/section-panel",
		"export": "SectionPanel"
	},
	"tags": [
		"panel",
		"section",
		"container",
		"card",
		"list",
		"dashboard",
		"scroll"
	],
	"intent": "A titled dashboard section container: an icon + title + count header with an optional right-aligned action, over a body that can be capped to a fixed pixel height and scroll internally. Compose it with Item rows for a compact, count-stable list (the 'assigned to you' pattern) or with cards — the panel's footprint stays constant no matter how many rows it holds.",
	"when": [
		"A dashboard area needs a titled, bordered container with a count and optional action",
		"A list whose length varies and must NOT change the page height — the body scrolls inside a fixed height",
		"Grouping several compact rows (Item) or small cards under one labelled section"
	],
	"whenNot": [
		"A single key metric tile -> use MetricCard",
		"A responsive grid of metric tiles -> use KPISummary",
		"A tabular, sortable, selectable record set -> use DataTable"
	],
	"chooseOver": [
		{
			"component": "A hand-rolled Card with a custom header + overflow div",
			"because": "SectionPanel owns the header (icon/title/count/action) and the fixed-height internal scroll so callers just pass rows."
		}
	],
	"pairsWith": [
		"Item",
		"Badge",
		"Button"
	],
	"requiredProviders": [],
	"dataContract": {
		"input": {
			"title": {
				"type": "string",
				"required": true,
				"note": "Header title."
			},
			"icon": {
				"type": "React.ElementType",
				"required": false,
				"note": "Leading header icon."
			},
			"count": {
				"type": "React.ReactNode",
				"required": false,
				"note": "Count badge next to the title."
			},
			"action": {
				"type": "React.ReactNode",
				"required": false,
				"note": "Right-aligned header slot (e.g. a Button)."
			},
			"height": {
				"type": "number",
				"required": false,
				"note": "Fixed body height in px; when set the body scrolls internally."
			},
			"empty": {
				"type": "React.ReactNode",
				"required": false,
				"note": "Rendered when there are no children."
			},
			"children": {
				"type": "React.ReactNode",
				"required": false,
				"note": "Rows or cards to render in the body."
			}
		},
		"note": "Presentational and stateless — the caller supplies the header metadata and the body content."
	},
	"states": {
		"default": "Header (icon + title + count + action) over a body sized to its content.",
		"scroll": "With `height` set, the body scrolls internally and the panel's footprint stays fixed.",
		"empty": "With no children, the `empty` slot renders in the body."
	},
	"composedOf": [
		"Card",
		"Badge",
		"ScrollArea"
	],
	"allowedPlacement": [
		"a section of a dashboard content region",
		"a column in a multi-panel dashboard grid"
	],
	"forbiddenPlacement": [
		"as a standalone page",
		"inside a table row"
	],
	"minimalExample": "import {SectionPanel} from \"@corensystem/coren-ui/section-panel\";\nimport {Item, ItemContent, ItemTitle, ItemDescription} from \"@corensystem/coren-ui/item\";\nimport {Bell} from \"lucide-react\";\n\n<SectionPanel icon={Bell} title=\"Needs Attention\" count={9} height={360}>\n  {rows.map((r) => (\n    <Item key={r.id}>\n      <ItemContent>\n        <ItemTitle>{r.name}</ItemTitle>\n        <ItemDescription>{r.detail}</ItemDescription>\n      </ItemContent>\n    </Item>\n  ))}\n</SectionPanel>",
	"lineage": {
		"origin": "authored",
		"authoredBy": "Extracted from the Timesheet summary sections (2026-07-13)",
		"derivedFrom": {
			"source": "packages/components/src/pages/core-verifytime-command-center.tsx",
			"extracted": [
				"titled section header with count badge + action",
				"fixed-height internal-scroll list body (count-stable footprint)"
			]
		},
		"status": "candidate",
		"verifiedBy": null,
		"confidence": "unset — no held-out eval yet",
		"supersedes": null,
		"lastValidated": "2026-07-13",
		"notes": [
			"pilot: true"
		]
	},
	"docs": {
		"usage": [
			"Use SectionPanel to structure and organize content within your interface. This component helps create visual hierarchy and consistent spacing throughout your application.",
			"SectionPanel works well for arranging related content, creating responsive layouts, and maintaining visual consistency. Use it to group related elements and establish clear content boundaries."
		],
		"props": [
			{
				"name": "icon",
				"type": "React.ElementType",
				"required": false,
				"description": "Leading header icon (lucide or any component type)."
			},
			{
				"name": "title",
				"type": "string",
				"required": true,
				"description": "Section title shown in the header."
			},
			{
				"name": "count",
				"type": "React.ReactNode",
				"required": false,
				"description": "Optional count badge next to the title (e.g. number of items)."
			},
			{
				"name": "action",
				"type": "React.ReactNode",
				"required": false,
				"description": "Optional element rendered at the far right of the header (e.g. a button)."
			},
			{
				"name": "height",
				"type": "number",
				"required": false,
				"description": "Fixed body height in pixels. When set, the body scrolls internally so the panel's footprint stays constant regardless of how many rows it holds — the list grows inward, not down the page."
			},
			{
				"name": "empty",
				"type": "React.ReactNode",
				"required": false,
				"description": "Rendered when there are no children (falsy)."
			},
			{
				"name": "children",
				"type": "React.ReactNode",
				"required": false,
				"description": ""
			},
			{
				"name": "bodyClassName",
				"type": "string",
				"required": false,
				"description": "Extra classes for the scrollable/content body."
			},
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "asChild",
				"type": "boolean",
				"default": "false",
				"required": false,
				"description": "Render as child element, merging props and behavior."
			}
		],
		"anatomy": {
			"description": "SectionPanel is composed of the following parts:",
			"parts": [
				{
					"name": "SectionPanel",
					"description": "Root SectionPanel element."
				},
				{
					"name": "SectionPanelContent",
					"description": "Content area of the SectionPanel."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Use semantic props",
				"description": "Use the component's built-in props rather than className overrides.",
				"example": "<SectionPanel variant=\"primary\" size=\"lg\" />"
			},
			{
				"type": "dont",
				"title": "Don't override core styles",
				"description": "Avoid using className to override the component's fundamental styling.",
				"example": "<SectionPanel className=\"custom-bg-color\" />"
			},
			{
				"type": "do",
				"title": "Provide accessible labels",
				"description": "Include proper ARIA labels for screen reader users.",
				"example": "<SectionPanel aria-label=\"Description\" />"
			},
			{
				"type": "dont",
				"title": "Don't skip accessibility",
				"description": "Never omit accessibility attributes for interactive components.",
				"example": "<SectionPanel /> // No aria-label"
			},
			{
				"type": "do",
				"title": "Handle all states",
				"description": "Account for loading, error, empty, and success states.",
				"example": "<SectionPanel loading={isLoading} error={error} />"
			},
			{
				"type": "dont",
				"title": "Don't ignore edge cases",
				"description": "Avoid showing broken UI when data is missing or loading.",
				"example": "<SectionPanel>{data.value}</SectionPanel> // Crashes if data is undefined"
			}
		]
	}
}
