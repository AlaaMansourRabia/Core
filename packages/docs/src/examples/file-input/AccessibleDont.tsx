/**
 * Avoid file inputs without labels or context.
 */
import {FileInput} from "@corensystem/coren-ui/file-input";

export function AccessibleDont() {
	return <FileInput id="file-input-accessible-dont" />;
}
