/**
 * Use code elements for actual code references.
 */
import {Code} from "@corensystem/coren-ui/code";

export function SemanticDo() {
	return (
		<p className="wwc:text-sm">
			The <Code>onClick</Code> handler fires on click.
		</p>
	);
}
