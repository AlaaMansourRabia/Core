{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "field",
	"tier": "component",
	"name": "Field",
	"category": "form",
	"version": "1.0.0",
	"manifestStatus": "complete",
	"source": {
		"path": "packages/components/src/field.tsx",
		"import": "@corensystem/coren-ui/field",
		"export": "Field"
	},
	"tags": [
		"form",
		"field",
		"wrapper"
	],
	"intent": "Field layout (label, control, description, error) wired to the Form context.",
	"when": [
		"Each control inside a Form"
	],
	"chooseOver": [],
	"pairsWith": [
		"Form",
		"Input",
		"Select"
	],
	"requiredProviders": [
		"Form"
	],
	"lineage": {
		"origin": "migrated",
		"authoredBy": "A1 component migration batch (2026-07)",
		"derivedFrom": {
			"source": "packages/components/src/field.tsx",
			"extracted": [
				"cva variants/sizes remain generated by the structural extractor"
			]
		},
		"status": "accepted",
		"verifiedBy": null,
		"confidence": "unset — projection round-trip verified; no held-out eval yet",
		"supersedes": null,
		"lastValidated": "2026-07-05",
		"notes": [
			"migrated from library-index.json (hand-authored catalog entry)"
		]
	},
	"docs": {
		"props": [
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "...props",
				"type": "React.HTMLAttributes<HTMLDivElement>",
				"required": false,
				"description": "All standard HTML attributes are supported."
			}
		],
		"usage": [
			"Use Field when you need to collect user input or allow users to make selections. This component provides a consistent interaction pattern that users will find familiar and accessible.",
			"Field should be used within forms or interactive interfaces where user data collection is required. Ensure proper labeling and validation feedback for accessibility. Avoid using this component for read-only or display-only content."
		],
		"anatomy": {
			"description": "Field is composed of the following parts:",
			"parts": [
				{
					"name": "Field",
					"description": "Root container element."
				},
				{
					"name": "FieldLabel",
					"description": "Label element for accessibility."
				},
				{
					"name": "FieldInput",
					"description": "Input element for user entry."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Always include labels",
				"description": "Every input should have an associated label for accessibility.",
				"example": "<Label htmlFor=\"email\">Email</Label>\n<Field id=\"email\" />"
			},
			{
				"type": "dont",
				"title": "Don't rely on placeholders alone",
				"description": "Placeholders disappear when typing and shouldn't replace labels.",
				"example": "<Field placeholder=\"Enter email\" />"
			},
			{
				"type": "do",
				"title": "Show validation feedback",
				"description": "Display clear error messages near the input when validation fails.",
				"example": "<Field aria-invalid=\"true\" />\n<span className=\"error\">Invalid email format</span>"
			},
			{
				"type": "dont",
				"title": "Don't delay error feedback",
				"description": "Show validation errors immediately, don't wait until form submission.",
				"example": "// Validating only on submit"
			},
			{
				"type": "do",
				"title": "Use appropriate input types",
				"description": "Use specialized input types (email, tel, number) for better mobile keyboards.",
				"example": "<Field type=\"email\" />"
			},
			{
				"type": "dont",
				"title": "Don't use text type for everything",
				"description": "Avoid using generic text inputs when a specialized type exists.",
				"example": "<Field type=\"text\" /> // for email"
			}
		]
	}
}
