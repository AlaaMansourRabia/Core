{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "sonner",
	"tier": "component",
	"name": "Sonner",
	"category": "feedback",
	"version": "1.0.0",
	"manifestStatus": "complete",
	"source": {
		"path": "packages/components/src/sonner.tsx",
		"import": "@corensystem/coren-ui/sonner",
		"export": "Sonner"
	},
	"tags": [
		"feedback",
		"toast",
		"notification"
	],
	"intent": "Transient toast notifications (success/error/info) that auto-dismiss.",
	"when": [
		"Confirm an async result without interrupting (Saved, Copied, Failed)"
	],
	"chooseOver": [
		{
			"component": "Alert",
			"because": "Alert for persistent inline messages"
		},
		{
			"component": "Dialog",
			"because": "Dialog when a decision/acknowledgement is required"
		}
	],
	"pairsWith": [],
	"requiredProviders": [
		"Toaster"
	],
	"commonMistakes": [
		{
			"mistake": "Not mounting a single <Toaster/> at the app root",
			"symptom": "toast() calls do nothing — no toast appears",
			"fix": "Mount one <Toaster/> once at the root",
			"avoidOverride": "Forget to mount a single <Toaster/> at the root — toasts won't appear"
		}
	],
	"lineage": {
		"origin": "migrated",
		"authoredBy": "A1 pilot — component-manifest migration (2026-07)",
		"derivedFrom": {
			"source": "packages/components/src/sonner.tsx",
			"extracted": [
				"cva variants/sizes remain generated by the structural extractor"
			]
		},
		"status": "accepted",
		"verifiedBy": null,
		"confidence": "unset — projection round-trip verified; no held-out eval yet",
		"supersedes": null,
		"lastValidated": "2026-07-05",
		"notes": [
			"migrated from library-index.json (hand-authored catalog entry)"
		]
	},
	"docs": {
		"props": [
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "...props",
				"type": "React.HTMLAttributes<HTMLElement>",
				"required": false,
				"description": "All standard HTML attributes are supported."
			}
		],
		"usage": [
			"Use Sonner to enhance your user interface. This component provides a consistent, accessible, and well-designed solution for this use case.",
			"Sonner integrates seamlessly with other components in the design system. Follow the established patterns and guidelines to ensure consistency across your application."
		],
		"anatomy": {
			"description": "Sonner is composed of the following parts:",
			"parts": [
				{
					"name": "Sonner",
					"description": "Root Sonner element."
				},
				{
					"name": "SonnerContent",
					"description": "Content area of the Sonner."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Use semantic props",
				"description": "Use the component's built-in props rather than className overrides.",
				"example": "<Sonner variant=\"primary\" size=\"lg\" />"
			},
			{
				"type": "dont",
				"title": "Don't override core styles",
				"description": "Avoid using className to override the component's fundamental styling.",
				"example": "<Sonner className=\"custom-bg-color\" />"
			},
			{
				"type": "do",
				"title": "Provide accessible labels",
				"description": "Include proper ARIA labels for screen reader users.",
				"example": "<Sonner aria-label=\"Description\" />"
			},
			{
				"type": "dont",
				"title": "Don't skip accessibility",
				"description": "Never omit accessibility attributes for interactive components.",
				"example": "<Sonner /> // No aria-label"
			},
			{
				"type": "do",
				"title": "Handle all states",
				"description": "Account for loading, error, empty, and success states.",
				"example": "<Sonner loading={isLoading} error={error} />"
			},
			{
				"type": "dont",
				"title": "Don't ignore edge cases",
				"description": "Avoid showing broken UI when data is missing or loading.",
				"example": "<Sonner>{data.value}</Sonner> // Crashes if data is undefined"
			}
		]
	}
}
