{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "map-control-panel",
	"tier": "widget",
	"name": "MapControlPanel",
	"category": "map",
	"version": "1.0.0",
	"manifestStatus": "stub",
	"source": {
		"path": "packages/components/src/map-control-panel.tsx",
		"import": "@corensystem/coren-ui/map-control-panel"
	},
	"tags": [
		"map",
		"controls",
		"filter",
		"collapsible",
		"overlay"
	],
	"intent": "Collapsible floating control card for a map — labelled dropdown filters over switch rows.",
	"when": [
		"A map needs basemap, filter and layer-visibility controls in one corner",
		"The controls must fold away so they stop covering the map"
	],
	"chooseOver": [
		{
			"component": "MapControls",
			"because": "MapControls is a horizontal strip of icon buttons with a built-in geocoder and geolocate. MapControlPanel is a labelled, collapsible card for filters that need to read as named fields rather than icons."
		}
	],
	"pairsWith": [
		"ObservationsMap",
		"MultiSelect",
		"Select"
	],
	"requiredProviders": [],
	"commonMistakes": [
		"Pre-selecting every option to mean \"show everything\" — an empty selection is the natural \"no filter\", and pre-selecting 90 zones fills the trigger with noise.",
		"Using the Select-module MultiSelect inside the panel — it renders its list as a plain absolutely-positioned element, which the card's overflow-hidden clips. The multi-select module's Popover-based MultiSelect portals out and carries the pills.",
		"Letting the panel grow with its rows — several filters plus their pills outgrow any map. The body scrolls under `maxBodyHeight` instead.",
		"Putting three options behind a dropdown — the click costs more than it saves. Use `as: \"chips\"` for short option sets and keep the dropdown for long ones.",
		"Letting a multi-select print every pill — eight selections push the rows below it off the panel. The pills cap at four and collapse the rest into a +N popover.",
		"Collapsing a panel to header height while keeping full width — it still occupies the surface it was meant to get out of the way of. Narrow it too.",
		"Forcing every chip row onto one line — five options at panel width truncate to nothing. Set chipColumns and let them wrap into an even grid.",
		"Masking a panel's scrolling body to fade its edges — that fades the content while the card's surface and border still end on a hard line. Mask the card itself."
	],
	"stubPlan": [
		"dataContract: stub — author input fields, slots, rowIdentity",
		"configSchema: stub — enumerate config props from source",
		"states: stub — list default/empty/loading/etc.",
		"composedOf: stub — list composed components",
		"evaluationCriteria: stub — define selection/config/render checks"
	],
	"lineage": {
		"origin": "authored",
		"authoredBy": "Observations-Map-View",
		"derivedFrom": {
			"source": null,
			"extracted": []
		},
		"status": "accepted",
		"verifiedBy": null,
		"confidence": "unset — every control exercised end-to-end in the browser; no held-out eval yet",
		"supersedes": null,
		"lastValidated": "2026-08-30",
		"notes": []
	},
	"composedOf": [
		"MultiSelect",
		"Select",
		"Chip",
		"Switch",
		"Label",
		"Button",
		"Badge"
	],
	"docs": {
		"usage": [
			"Use MapControlPanel to structure and organize content within your interface. This component helps create visual hierarchy and consistent spacing throughout your application.",
			"MapControlPanel works well for arranging related content, creating responsive layouts, and maintaining visual consistency. Use it to group related elements and establish clear content boundaries."
		],
		"props": [
			{
				"name": "title",
				"type": "React.ReactNode",
				"required": false,
				"description": "Header text. Default \"Map\"."
			},
			{
				"name": "selects",
				"type": "MapControlPanelSelect[]",
				"required": false,
				"description": "Dropdown rows, in order."
			},
			{
				"name": "toggles",
				"type": "MapControlPanelToggle[]",
				"required": false,
				"description": "Switch rows, rendered under the dropdowns."
			},
			{
				"name": "defaultCollapsed",
				"type": "boolean",
				"required": false,
				"description": "Start collapsed to just the header. Default false."
			},
			{
				"name": "collapsed",
				"type": "boolean",
				"required": false,
				"description": "Collapsed state (controlled)."
			},
			{
				"name": "hasActiveFilters",
				"type": "boolean",
				"required": false,
				"description": "Whether any filter is set. Defaults to reading the rows: any `selects` row with a selection. Pass it explicitly when a filter lives outside `selects`."
			},
			{
				"name": "width",
				"type": "number",
				"required": false,
				"description": "Panel width in px. Default `340`, matching the floating detail panels so the two agree."
			},
			{
				"name": "collapsedWidth",
				"type": "number",
				"required": false,
				"description": "Width in px while collapsed. The header carries only a title and the toggle, so it has no reason to hold the full width. Default `160`."
			},
			{
				"name": "maxHeight",
				"type": "string",
				"required": false,
				"description": "Cap on the whole card's height, as a CSS length; the body scrolls beneath it. Default `\"60vh\"`."
			},
			{
				"name": "fadeEdges",
				"type": "boolean",
				"required": false,
				"description": "Fade the whole card out at its top and bottom edges — surface, border and all — so it dissolves into what is behind it instead of ending on a hard rectangle."
			},
			{
				"name": "children",
				"type": "React.ReactNode",
				"required": false,
				"description": "Extra rows below the built-in ones."
			},
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "...props",
				"type": "React.HTMLAttributes<HTMLDivElement>",
				"required": false,
				"description": "All standard HTML attributes are supported."
			}
		],
		"anatomy": {
			"description": "MapControlPanel is composed of the following parts:",
			"parts": [
				{
					"name": "MapControlPanel",
					"description": "Root MapControlPanel element."
				},
				{
					"name": "MapControlPanelContent",
					"description": "Content area of the MapControlPanel."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Use semantic props",
				"description": "Use the component's built-in props rather than className overrides.",
				"example": "<MapControlPanel variant=\"primary\" size=\"lg\" />"
			},
			{
				"type": "dont",
				"title": "Don't override core styles",
				"description": "Avoid using className to override the component's fundamental styling.",
				"example": "<MapControlPanel className=\"custom-bg-color\" />"
			},
			{
				"type": "do",
				"title": "Provide accessible labels",
				"description": "Include proper ARIA labels for screen reader users.",
				"example": "<MapControlPanel aria-label=\"Description\" />"
			},
			{
				"type": "dont",
				"title": "Don't skip accessibility",
				"description": "Never omit accessibility attributes for interactive components.",
				"example": "<MapControlPanel /> // No aria-label"
			},
			{
				"type": "do",
				"title": "Handle all states",
				"description": "Account for loading, error, empty, and success states.",
				"example": "<MapControlPanel loading={isLoading} error={error} />"
			},
			{
				"type": "dont",
				"title": "Don't ignore edge cases",
				"description": "Avoid showing broken UI when data is missing or loading.",
				"example": "<MapControlPanel>{data.value}</MapControlPanel> // Crashes if data is undefined"
			}
		]
	}
}
