/**
 * Avoid hidden handles.
 */
import {TimelineRangeSelector} from "@corensystem/coren-ui/timeline-range-selector";

export function HandlesDont() {
	return <TimelineRangeSelector start={20} end={60} />;
}
