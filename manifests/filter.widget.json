{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "filter",
	"tier": "widget",
	"name": "Filter",
	"category": "data",
	"version": "1.0.0",
	"manifestStatus": "stub",
	"source": {
		"path": "packages/components/src/filter.tsx",
		"import": "@corensystem/coren-ui/filter"
	},
	"tags": [
		"filter",
		"facets",
		"popover",
		"multi-select"
	],
	"intent": "Faceted multi-category filter — an icon trigger opening a popover of collapsible categories with selectable options.",
	"when": [
		"Filter a list or table by several independent categories at once",
		"Offer compact faceted filtering behind a trigger"
	],
	"chooseOver": [
		{
			"component": "SearchFilterBar",
			"because": "SearchFilterBar is a single-row search plus quick filter pills; Filter is a multi-category faceted picker in a popover"
		},
		{
			"component": "Combobox",
			"because": "Combobox selects a single field's value; Filter manages many categories of multi-select facets"
		}
	],
	"pairsWith": [
		"DataTable",
		"SearchFilterBar"
	],
	"requiredProviders": [
		"Filter"
	],
	"commonMistakes": [
		{
			"mistake": "Render FilterOption outside FilterCategory, or FilterCategory outside Filter",
			"fix": "Both require their context and throw without it",
			"avoidOverride": "Render FilterOption outside FilterCategory, or FilterCategory outside Filter — both require their context and throw without it"
		}
	],
	"stubPlan": [
		"dataContract: stub — author input fields, slots, rowIdentity",
		"configSchema: stub — enumerate config props from source",
		"states: stub — list default/empty/loading/etc.",
		"composedOf: stub — list composed components",
		"evaluationCriteria: stub — define selection/config/render checks"
	],
	"lineage": {
		"origin": "migrated",
		"authoredBy": "bulk widget promotion (2026-06)",
		"derivedFrom": {
			"source": null,
			"extracted": []
		},
		"status": "accepted",
		"verifiedBy": null,
		"confidence": "unset — projection round-trip verified; no held-out eval yet",
		"supersedes": null,
		"lastValidated": "2026-07-05",
		"notes": [
			"promotedFrom: component (library-index.json)",
			"pilot: false",
			"knowledgeRefs: [\"library-index.json#Filter\"]"
		]
	},
	"docs": {
		"usage": [
			"Use Filter to enhance your user interface. This component provides a consistent, accessible, and well-designed solution for this use case.",
			"Filter integrates seamlessly with other components in the design system. Follow the established patterns and guidelines to ensure consistency across your application."
		],
		"props": [
			{
				"name": "value",
				"type": "FilterValue",
				"required": false,
				"description": "Controlled applied value."
			},
			{
				"name": "defaultValue",
				"type": "FilterValue",
				"required": false,
				"description": "Uncontrolled initial value."
			},
			{
				"name": "children",
				"type": "React.ReactNode",
				"required": true,
				"description": ""
			},
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "asChild",
				"type": "boolean",
				"default": "false",
				"required": false,
				"description": "Render as child element, merging props and behavior."
			},
			{
				"name": "disabled",
				"type": "boolean",
				"default": "false",
				"required": false,
				"description": "Whether the component is disabled."
			},
			{
				"name": "...props",
				"type": "React.HTMLAttributes<HTMLDivElement>",
				"required": false,
				"description": "All standard HTML attributes are supported."
			}
		],
		"anatomy": {
			"description": "Filter is composed of the following parts:",
			"parts": [
				{
					"name": "Filter",
					"description": "Root Filter element."
				},
				{
					"name": "FilterContent",
					"description": "Content area of the Filter."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Use semantic props",
				"description": "Use the component's built-in props rather than className overrides.",
				"example": "<Filter variant=\"primary\" size=\"lg\" />"
			},
			{
				"type": "dont",
				"title": "Don't override core styles",
				"description": "Avoid using className to override the component's fundamental styling.",
				"example": "<Filter className=\"custom-bg-color\" />"
			},
			{
				"type": "do",
				"title": "Provide accessible labels",
				"description": "Include proper ARIA labels for screen reader users.",
				"example": "<Filter aria-label=\"Description\" />"
			},
			{
				"type": "dont",
				"title": "Don't skip accessibility",
				"description": "Never omit accessibility attributes for interactive components.",
				"example": "<Filter /> // No aria-label"
			},
			{
				"type": "do",
				"title": "Handle all states",
				"description": "Account for loading, error, empty, and success states.",
				"example": "<Filter loading={isLoading} error={error} />"
			},
			{
				"type": "dont",
				"title": "Don't ignore edge cases",
				"description": "Avoid showing broken UI when data is missing or loading.",
				"example": "<Filter>{data.value}</Filter> // Crashes if data is undefined"
			}
		]
	}
}
