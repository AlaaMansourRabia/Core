{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "radio-group",
	"tier": "component",
	"name": "RadioGroup",
	"category": "primitives",
	"version": "1.0.0",
	"manifestStatus": "complete",
	"source": {
		"path": "packages/components/src/radio-group.tsx",
		"import": "@corensystem/coren-ui/radio-group",
		"export": "RadioGroup"
	},
	"tags": [
		"form",
		"input",
		"selection",
		"single"
	],
	"intent": "Choose exactly one option from a small, visible set.",
	"when": [
		"2–5 mutually-exclusive options that should all be visible"
	],
	"chooseOver": [
		{
			"component": "Select",
			"because": "Select/Combobox when there are many options or space is tight"
		},
		{
			"component": "ToggleGroup",
			"because": "ToggleGroup for an inline segmented control"
		},
		{
			"component": "Switch",
			"because": "Switch/Checkbox for a boolean, not a one-of-many"
		}
	],
	"pairsWith": [
		"Form",
		"Field"
	],
	"requiredProviders": [],
	"commonMistakes": [
		{
			"mistake": "Using RadioGroup for many options",
			"fix": "Use Select",
			"avoidOverride": "Use for many options — use Select"
		}
	],
	"lineage": {
		"origin": "migrated",
		"authoredBy": "A1 component migration batch (2026-07)",
		"derivedFrom": {
			"source": "packages/components/src/radio-group.tsx",
			"extracted": [
				"cva variants/sizes remain generated by the structural extractor"
			]
		},
		"status": "accepted",
		"verifiedBy": null,
		"confidence": "unset — projection round-trip verified; no held-out eval yet",
		"supersedes": null,
		"lastValidated": "2026-07-05",
		"notes": [
			"migrated from library-index.json (hand-authored catalog entry)"
		]
	},
	"docs": {
		"props": [
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "disabled",
				"type": "boolean",
				"default": "false",
				"required": false,
				"description": "Whether the component is disabled."
			},
			{
				"name": "...props",
				"type": "React.HTMLAttributes<HTMLElement>",
				"required": false,
				"description": "All standard HTML attributes are supported."
			}
		],
		"usage": [
			"Use RadioGroup when you need to collect user input or allow users to make selections. This component provides a consistent interaction pattern that users will find familiar and accessible.",
			"RadioGroup should be used within forms or interactive interfaces where user data collection is required. Ensure proper labeling and validation feedback for accessibility. Avoid using this component for read-only or display-only content."
		],
		"anatomy": {
			"description": "RadioGroup is composed of the following parts:",
			"parts": [
				{
					"name": "RadioGroup",
					"description": "Root RadioGroup element."
				},
				{
					"name": "RadioGroupContent",
					"description": "Content area of the RadioGroup."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Use semantic props",
				"description": "Use the component's built-in props rather than className overrides.",
				"example": "<RadioGroup variant=\"primary\" size=\"lg\" />"
			},
			{
				"type": "dont",
				"title": "Don't override core styles",
				"description": "Avoid using className to override the component's fundamental styling.",
				"example": "<RadioGroup className=\"custom-bg-color\" />"
			},
			{
				"type": "do",
				"title": "Provide accessible labels",
				"description": "Include proper ARIA labels for screen reader users.",
				"example": "<RadioGroup aria-label=\"Description\" />"
			},
			{
				"type": "dont",
				"title": "Don't skip accessibility",
				"description": "Never omit accessibility attributes for interactive components.",
				"example": "<RadioGroup /> // No aria-label"
			},
			{
				"type": "do",
				"title": "Handle all states",
				"description": "Account for loading, error, empty, and success states.",
				"example": "<RadioGroup loading={isLoading} error={error} />"
			},
			{
				"type": "dont",
				"title": "Don't ignore edge cases",
				"description": "Avoid showing broken UI when data is missing or loading.",
				"example": "<RadioGroup>{data.value}</RadioGroup> // Crashes if data is undefined"
			}
		]
	}
}
