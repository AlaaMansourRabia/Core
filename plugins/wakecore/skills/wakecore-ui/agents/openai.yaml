interface:
  display_name: "WakeCore UI"
  short_description: "Bootstrap and build approved WakeCore React UI"
  brand_color: "#F36F45"
  default_prompt: "Use $wakecore-ui to create the React app if needed, build this interface from WakeCore, and pass WakeCore-only validation."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: mcp
      value: "wakecore"
      description: "Hosted WakeCore template resolution, implementation planning, and validation tools."
