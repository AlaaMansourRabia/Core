{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "chip",
	"tier": "component",
	"name": "Chip",
	"category": "form",
	"version": "1.0.0",
	"manifestStatus": "complete",
	"source": {
		"path": "packages/components/src/chip.tsx",
		"import": "@corensystem/coren-ui/chip",
		"export": "Chip"
	},
	"tags": [
		"chip",
		"filter",
		"tag",
		"attachment"
	],
	"intent": "Compact pill for a toggleable filter selection or a static attachment/context item.",
	"when": [
		"Toggle a filter value on/off within a filter bar",
		"Display an attached file or context item as a removable pill"
	],
	"chooseOver": [
		{
			"component": "Badge",
			"because": "Badge is a non-interactive status label; the filter Chip is toggleable (selected/unselected state)"
		},
		{
			"component": "Toggle",
			"because": "Toggle is a standalone on/off control; Chip is styled for filter and attachment lists"
		}
	],
	"pairsWith": [
		"SearchFilterBar",
		"Filter"
	],
	"requiredProviders": [],
	"commonMistakes": [
		{
			"mistake": "Using Chip a filter Chip for non-interactive status",
			"fix": "Use Badge instead",
			"avoidOverride": "Use a filter Chip for non-interactive status — use Badge instead"
		}
	],
	"variantIntent": {
		"filter": "Toggleable filter chip with selected/unselected states",
		"attachment": "Static chip displaying an attached file or context item"
	},
	"lineage": {
		"origin": "migrated",
		"authoredBy": "A1 component migration batch (2026-07)",
		"derivedFrom": {
			"source": "packages/components/src/chip.tsx",
			"extracted": [
				"cva variants/sizes remain generated by the structural extractor"
			]
		},
		"status": "accepted",
		"verifiedBy": null,
		"confidence": "unset — projection round-trip verified; no held-out eval yet",
		"supersedes": null,
		"lastValidated": "2026-07-05",
		"notes": [
			"migrated from library-index.json (hand-authored catalog entry)"
		]
	},
	"docs": {
		"props": [
			{
				"name": "variant",
				"type": "\"filter\" | \"attachment\"",
				"default": "\"filter\"",
				"required": false,
				"description": "Visual style variant."
			},
			{
				"name": "size",
				"type": "\"sm\" | \"default\" | \"lg\"",
				"default": "\"default\"",
				"required": false,
				"description": "Size variant."
			},
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "children",
				"type": "React.ReactNode",
				"required": true,
				"description": "Child elements to render."
			},
			{
				"name": "asChild",
				"type": "boolean",
				"default": "false",
				"required": false,
				"description": "Render as child element, merging props and behavior."
			},
			{
				"name": "disabled",
				"type": "boolean",
				"default": "false",
				"required": false,
				"description": "Whether the component is disabled."
			},
			{
				"name": "...props",
				"type": "React.HTMLAttributes<HTMLSpanElement>",
				"required": false,
				"description": "All standard HTML attributes are supported."
			}
		],
		"usage": [
			"Use Chip to present visual information or status indicators to users. This component helps convey information quickly through visual representation.",
			"Chip is effective for showing user identity, status, categories, or labels. Keep usage consistent throughout your application to help users quickly recognize and understand the information being displayed."
		],
		"anatomy": {
			"description": "Chip is composed of the following parts:",
			"parts": [
				{
					"name": "Chip",
					"description": "Root Chip element."
				},
				{
					"name": "ChipContent",
					"description": "Content area of the Chip."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Use semantic props",
				"description": "Use the component's built-in props rather than className overrides.",
				"example": "<Chip variant=\"primary\" size=\"lg\" />"
			},
			{
				"type": "dont",
				"title": "Don't override core styles",
				"description": "Avoid using className to override the component's fundamental styling.",
				"example": "<Chip className=\"custom-bg-color\" />"
			},
			{
				"type": "do",
				"title": "Provide accessible labels",
				"description": "Include proper ARIA labels for screen reader users.",
				"example": "<Chip aria-label=\"Description\" />"
			},
			{
				"type": "dont",
				"title": "Don't skip accessibility",
				"description": "Never omit accessibility attributes for interactive components.",
				"example": "<Chip /> // No aria-label"
			},
			{
				"type": "do",
				"title": "Handle all states",
				"description": "Account for loading, error, empty, and success states.",
				"example": "<Chip loading={isLoading} error={error} />"
			},
			{
				"type": "dont",
				"title": "Don't ignore edge cases",
				"description": "Avoid showing broken UI when data is missing or loading.",
				"example": "<Chip>{data.value}</Chip> // Crashes if data is undefined"
			}
		]
	}
}
