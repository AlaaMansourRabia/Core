import {Badge} from "@corensystem/coren-ui/badge";

/** Outline variant with subtle border styling. */
export function Outline() {
	return (
		<Badge id="badge-outline" variant="outline">
			Badge
		</Badge>
	);
}
