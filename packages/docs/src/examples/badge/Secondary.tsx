import {Badge} from "@corensystem/coren-ui/badge";

/** Secondary variant with muted styling. */
export function Secondary() {
	return (
		<Badge id="badge-secondary" variant="secondary">
			Badge
		</Badge>
	);
}
