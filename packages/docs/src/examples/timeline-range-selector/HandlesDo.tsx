/**
 * Use clear range handles.
 */
import {TimelineRangeSelector} from "@corensystem/coren-ui/timeline-range-selector";

export function HandlesDo() {
	return <TimelineRangeSelector start={20} end={60} showHandles />;
}
