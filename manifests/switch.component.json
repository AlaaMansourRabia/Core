{
	"schemaVersion": "artifact-manifest/0.2",
	"id": "switch",
	"tier": "component",
	"name": "Switch",
	"category": "primitives",
	"version": "1.0.0",
	"manifestStatus": "complete",
	"source": {
		"path": "packages/components/src/switch.tsx",
		"import": "@corensystem/coren-ui/switch",
		"export": "Switch"
	},
	"tags": [
		"form",
		"input",
		"boolean",
		"toggle"
	],
	"intent": "Instantly toggle a single setting on or off.",
	"when": [
		"Settings that apply immediately (notifications on/off)"
	],
	"chooseOver": [
		{
			"component": "Checkbox",
			"because": "Checkbox for selection submitted with a form; Switch for immediate settings"
		},
		{
			"component": "Toggle",
			"because": "Toggle is a toolbar button-press; Switch is a labeled setting"
		}
	],
	"pairsWith": [
		"Form",
		"Label"
	],
	"requiredProviders": [],
	"commonMistakes": [
		{
			"mistake": "Using Switch inside a multi-select list",
			"fix": "Use Checkbox",
			"avoidOverride": "Use inside a multi-select list — use Checkbox"
		}
	],
	"lineage": {
		"origin": "migrated",
		"authoredBy": "A1 component migration batch (2026-07)",
		"derivedFrom": {
			"source": "packages/components/src/switch.tsx",
			"extracted": [
				"cva variants/sizes remain generated by the structural extractor"
			]
		},
		"status": "accepted",
		"verifiedBy": null,
		"confidence": "unset — projection round-trip verified; no held-out eval yet",
		"supersedes": null,
		"lastValidated": "2026-07-05",
		"notes": [
			"migrated from library-index.json (hand-authored catalog entry)"
		]
	},
	"docs": {
		"props": [
			{
				"name": "size",
				"type": "\"default\" | \"sm\"",
				"required": false,
				"description": "`sm` for dense surfaces — floating panels, toolbars, table rows. Default `default`."
			},
			{
				"name": "className",
				"type": "string",
				"required": false,
				"description": "Additional CSS classes to apply."
			},
			{
				"name": "disabled",
				"type": "boolean",
				"default": "false",
				"required": false,
				"description": "Whether the component is disabled."
			},
			{
				"name": "...props",
				"type": "React.HTMLAttributes<HTMLElement>",
				"required": false,
				"description": "All standard HTML attributes are supported."
			}
		],
		"usage": [
			"Use Switch when you need to collect user input or allow users to make selections. This component provides a consistent interaction pattern that users will find familiar and accessible.",
			"Switch should be used within forms or interactive interfaces where user data collection is required. Ensure proper labeling and validation feedback for accessibility. Avoid using this component for read-only or display-only content."
		],
		"anatomy": {
			"description": "Switch is composed of the following parts:",
			"parts": [
				{
					"name": "Switch",
					"description": "Root Switch element."
				},
				{
					"name": "SwitchContent",
					"description": "Content area of the Switch."
				}
			]
		},
		"bestPractices": [
			{
				"type": "do",
				"title": "Use semantic props",
				"description": "Use the component's built-in props rather than className overrides.",
				"example": "<Switch variant=\"primary\" size=\"lg\" />"
			},
			{
				"type": "dont",
				"title": "Don't override core styles",
				"description": "Avoid using className to override the component's fundamental styling.",
				"example": "<Switch className=\"custom-bg-color\" />"
			},
			{
				"type": "do",
				"title": "Provide accessible labels",
				"description": "Include proper ARIA labels for screen reader users.",
				"example": "<Switch aria-label=\"Description\" />"
			},
			{
				"type": "dont",
				"title": "Don't skip accessibility",
				"description": "Never omit accessibility attributes for interactive components.",
				"example": "<Switch /> // No aria-label"
			},
			{
				"type": "do",
				"title": "Handle all states",
				"description": "Account for loading, error, empty, and success states.",
				"example": "<Switch loading={isLoading} error={error} />"
			},
			{
				"type": "dont",
				"title": "Don't ignore edge cases",
				"description": "Avoid showing broken UI when data is missing or loading.",
				"example": "<Switch>{data.value}</Switch> // Crashes if data is undefined"
			}
		]
	}
}
